numeros = [10, 30, 15, 5, 15, 10, 40]

# Retorna o menor elemento da lista
numeros_min = min(numeros) # 5

# Retorna o maior elemento da lista
numeros_max = max(numeros) # 40

# Retorma a soma dos elementos
numeros_sum = sum(numeros) # 125

# Orderna a lista
# sorted(numeros)

numeros_sort = numeros.sort()

print(numeros_sort)

inverso = list(reversed(numeros))


print(numeros_sum) # 5


tamanho = len(numeros)

ordenada = sorted(numeros)

inverso = list(reversed(numeros))
